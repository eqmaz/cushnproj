package config

// defaultConfigMap provides default configuration values.
// Users should define their own defaults in the application-specific cfg code.
var defaultConfigMap = map[string]interface{}{
	// Example:
	// "exampleKey": "exampleValue",
}
