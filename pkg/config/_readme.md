# Config loader package

Simple package to search for and load one config file
Environment variables will override the config file 
In the absence of a config file, defaults can be used.
Defaults are overridden by environment variables.

Work in progress